# URL base da API do backend (FastAPI)
VITE_API_URL=http://localhost:8000

# Chave publicável da Stripe — não é usada hoje (o checkout é 100% redirect-based,
# o frontend só navega para a URL que o backend devolve), documentada aqui só
# para uso futuro se algum fluxo passar a usar o Stripe.js diretamente no navegador.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_substitua
